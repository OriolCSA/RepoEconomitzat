'use strict';

/**
 * notificacio-llegides-usuari router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notificacio-llegides-usuari.notificacio-llegides-usuari');
