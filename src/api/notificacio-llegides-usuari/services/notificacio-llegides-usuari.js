'use strict';

/**
 * notificacio-llegides-usuari service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::notificacio-llegides-usuari.notificacio-llegides-usuari');
