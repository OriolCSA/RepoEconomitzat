'use strict';

/**
 * notificacio-llegides-usuari controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::notificacio-llegides-usuari.notificacio-llegides-usuari');
